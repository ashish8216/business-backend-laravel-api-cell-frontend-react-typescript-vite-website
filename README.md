# Business Backend (Laravel API) & Frontend (React + TypeScript + Vite)

This repository contains a **full-stack business website** built using:
- **Backend:** Laravel (REST API)
- **Frontend:** React + TypeScript + Vite
- **Architecture:** Headless setup — the Laravel API powers the React frontend via JSON endpoints.

---
